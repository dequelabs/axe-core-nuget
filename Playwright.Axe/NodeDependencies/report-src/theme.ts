import { createTheme, PartialTheme } from "@fluentui/react";

/**
 * Default Theme
 */
const defaultTheme: PartialTheme = createTheme({
    semanticColors: {
        link: "#006cbe"
    }
})

export { defaultTheme }